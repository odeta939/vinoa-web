type Wine = {
  name: string;
  // slug?: {slug: string};
  // image?: {asset: {url: string}};
  // tastedAt?: Date;
  country: string;
  region: string;
  smell: string;
  taste: string;
  colour: string;
}